name: Caido SAST Security Scan

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  security-scan:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      # Caidoサーバーのダウンロード＆起動は不要になりました！一気にスキャンを実行します。
      - name: Run Caido Rule Engine
        run: |
          echo '{"findings": []}' > results.json
          node .caido/run-scan.js
        continue-on-error: true

      - name: Debug results.json
        if: always()
        run: |
          echo "=== results.json の中身を表示します ==="
          [ -f results.json ] && cat results.json || echo "ファイルが存在しません"

      - name: Post Result Comment
        if: matrix.os == 'ubuntu-latest' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            let body = "### 🛡️ Caido Security Scan Result\n\n";
            try {
              if (fs.existsSync('results.json')) {
                const content = fs.readFileSync('results.json', 'utf8').trim();
                const data = JSON.parse(content);
                
                // 【修正】エラーを正しく検知して表示するように変更
                if (data.error) {
                  body += "⚠️ スキャン実行中にエラーが発生しました。\n\n**エラー内容:**\n```\n" + data.error + "\n```";
                } else {
                  const findings = data.findings || [];
                  if (findings.length === 0) {
                    body += "✅ 脆弱性は見つかりませんでした。";
                  } else {
                    body += "| Severity | File | Line | Message |\n| :--- | :--- | :--- | :--- |\n";
                    findings.forEach(f => {
                      const icon = f.severity === 'CRITICAL' ? '🔴' : '🟡';
                      body += `| ${icon} ${f.severity} | \`${f.file}\` | ${f.line} | ${f.message} |\n`;
                    });
                  }
                }
              } else {
                body += "⚠️ スキャン結果ファイルが生成されませんでした。";
              }
            } catch (e) {
              body += "❌ 結果の解析中に予期せぬエラーが発生しました。\n" + e.message;
            }
            
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });